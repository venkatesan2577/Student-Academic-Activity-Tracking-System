SELECT
    s.register_no,
    s.student_name,
    s.department,
    a.semester,
    ROUND(AVG(a.marks), 2) AS average_marks
FROM students s
JOIN academics a
ON s.student_id = a.student_id
GROUP BY
    s.register_no,
    s.student_name,
    s.department,
    a.semester
ORDER BY average_marks DESC;

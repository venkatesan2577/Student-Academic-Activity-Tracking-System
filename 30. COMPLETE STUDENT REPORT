SELECT
    s.register_no,
    s.student_name,
    s.department,
    s.year_of_study,
    s.cgpa,
    p.company_name,
    p.job_role,
    p.package_lpa,
    p.placement_status
FROM students s
LEFT JOIN placements p
ON s.student_id = p.student_id
ORDER BY s.cgpa DESC;

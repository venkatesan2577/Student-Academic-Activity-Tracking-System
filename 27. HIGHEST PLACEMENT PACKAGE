SELECT
    s.student_name,
    p.company_name,
    p.job_role,
    p.package_lpa
FROM students s
JOIN placements p
ON s.student_id = p.student_id
WHERE p.package_lpa = (
    SELECT MAX(package_lpa)
    FROM placements
);
